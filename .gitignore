testdata/
*.log
WebView2Cache/
.playwright-mcp/

# Authoring byproducts of the deck pipeline (kept locally, not published)
_audit/
docs/*/analysis/
docs/*/validation/
docs/*/backup/
docs/*/live_preview/

# One run = one case. Both trees are produced by running the app.
exports/
temp/
