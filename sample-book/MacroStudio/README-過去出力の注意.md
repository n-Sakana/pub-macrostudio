# 過去の生成例について

このディレクトリの成果物は、元の ZIP に含まれていた過去の参照資料です。
今回の修正版で作成したファイルではなく、内容は原本のまま保管しています。

`sample_win32_sleep-Modified-20260731.xlsm` には、VBA のソースが変更されている一方で
元と同じ実行キャッシュが残っていることを、バイナリ比較で確認しました。
Excel で何が実行されるかの実機検証は今回行っていません。

検証時は、上位フォルダの元の `sample_win32_sleep.xlsm` のコピーから、修正版アプリで
新しい成果物を作成してください。この過去の生成ブックを「修正済み」として使用したり、
マクロを不用意に実行したりしないでください。

詳細はルートの `BUG_CHECK_REPORT.md` を参照してください。
