# MacroStudio バグチェック・修正報告書

- 対象: 提供された `macrostudio.zip` の MacroStudio beta 1.1.0
- 修正版識別: **beta 1.1.1-audit.1**
- 報告日: 2026-09-08
- 元 ZIP の SHA-256: `72564209590b69f8e25a1fb12a38d05efba5e79577141d4209c54200cc6a6718`

## 1. 結論

動作・取り込み検査・状態管理・ファイル保全に、修正すべき問題がありました。
特に、**編集済み依頼に対して古い返答が有効なまま残ること、同時刻の依頼ファイルの
上書き、再ビルド時の固定名ファイルの削除、VBA 実行キャッシュの残存**は、
意図しない成果物や既存データの損失につながる重要な問題です。

修正したソース、起動スクリプト、必要な同梱 DLL、プリセット、テンプレート、
元のサンプル資料、検証スクリプトを修正 ZIP に収録しました。
報告書も ZIP のルートに含めています。

**今回の実行確認は JavaScript と Chromium 上の画面検証までです。**
Windows / .NET Framework / WPF / WebView2 の実機、Excel はこの環境にないため、
変更後の C# をコンパイル・実行していません。C# 側の変更はコードレビューと仕様確認に
基づく実装修正であり、Windows / Excel の検証完了を意味しません。
業務利用前に、同梱の Windows 用テストと実ブックのコピーで確認してください。

## 2. 確認範囲と方法

対象は MacroStudio アプリ本体です。任意の Excel ブックに含まれるマクロの業務仕様や、
外部 AI が返すコードの正しさまで保証する検査ではありません。

| 領域 | 今回実施したこと |
|---|---|
| 構成・起動 | VBS / PowerShell、C# 全ファイル、WebView2 の初期化・メッセージ経路を静的確認。 |
| VBA 読み書き | OLE2 / VBA 圧縮・ソース抽出・書き戻し・変更外データ検証の実装を確認。提供サンプルの CFB ストリームを独立した Python 処理で比較。 |
| AI との受け渡し | 同梱 6 プリセット、テンプレート置換、依頼番号、返答構文、分割返答、変更なし回答をテスト。 |
| 状態と操作 | 通常・簡易・相談の遷移、編集による失効、戻る経路に関連する状態、再取り込み、エラー・再試行、二重実行、複数ドロップを確認。 |
| 表示 | 長い差分、名前エラー、通知、小さい画面領域、テーマ、単独 HTML レポートを確認。 |
| データ保全 | 元ブックの読み直し、出力の公開・置換・後始末、既存成果物の扱い、文字エンコードを確認。 |

### 実行環境

Linux / Node.js 22.16.0 / Python 3.13.5 / Playwright 1.57.0 /
Chromium 144.0.7559.96。

ブラウザーテストは、パッケージの実際の HTML・CSS・JavaScript をローカルから直接
読み込み、Windows ホスト・保存・クリップボード・アセット取得を代替処理にしています。
画面コードや差分生成コードは実物ですが、Windows の API や実ファイル書き戻しを
検証したものではありません。外部 AI への送信、Excel / VBA の実行はしていません。

## 3. 重要な修正の詳細

### B01 — 古い VBA 実行キャッシュが残る【高】

**元の処理:** 圧縮ソースを書き換えても、`_VBA_PROJECT` のバージョンとキャッシュを
そのまま保っていました。書き戻したソースが一致するだけでは、Excel が参照する
実行キャッシュとの一致まで確認できません。

**提供サンプルで確認した事実:** 元ブックと、入力 ZIP 内の過去の改修済みブックを
独立した CFB 読み取り処理で比較しました。`WindowUtils`、`TimerUtils`、`SystemInfo`、
`AppController` などのストリームが変わっている一方、`_VBA_PROJECT` は
**6,308 バイトすべて同一**で、先頭 7 バイトは双方とも `cc61b500000300` でした。
この事実はキャッシュの残存を示しますが、Excel でどのコードが実行されたかを
今回実測したものではありません。

**修正:** ソースの変更・モジュール追加時に `_VBA_PROJECT` を
`CC 61 FF FF 00 00 00` のヘッダーへ置き換え、プロジェクトの PerformanceCache を
除去します。バージョン `0xFFFF` によるキャッシュ無効化は Microsoft の MS-OVBA
仕様に基づきます [S1][S2]。変更後を読み直して、このヘッダーまで検証する処理を追加しました。
個々のモジュールの既存プレフィックスは保持しますが、古いキャッシュを有効扱いする
バージョンのままにはしません。

**場所:** `src/07_VbaProject.cs` の `RebuildProject` / `CreateInvalidatedCache`、
`src/08_BookIO.cs` の `VerifyBuild`。

**確認状況:** 元サンプルのバイナリ比較と仕様照合済み。修正版の書き戻し・Excel 再コンパイルは
未実行。`tests/Test-Windows.ps1` にソース往復・ヘッダー検査を追加しています。

### B02–B04 — 既存ファイルを失う可能性【高】

| 番号 | 元の問題 | 修正 |
|---|---|---|
| B02 | 同じブックで同じ秒に依頼を作ると `<ブック名>_<日時>` フォルダを再利用し、`request.md` / `source-code.md` を上書きし得る。 | 実行ごとのランダム識別子を追加。新規の依頼ファイルは上書き禁止で作成し、2 ファイルが揃ってから現在の実行として確定。失敗時は自分で作成したファイルだけを後始末。 |
| B03 | 再ビルド時に出力名へ `.rebuild` / `.previous` を付けた固定名を使い、同名ファイルを削除し得る。 | 推測できる固定名を廃止。同一ディレクトリの一意な一時ファイルを `CreateNew` で作り、検証後のみ公開。既存出力の置換は `File.Replace`、新規公開は上書きしない `File.Move` を使用 [S3]。 |
| B04 | レポートや概要メモの再保存時に、以前の内容を切り詰めてから書くため、途中失敗で以前の成果物が失われる。 | テキストも一時ファイルへ書いてから公開する方式へ統一。置換失敗時の復元・バックアップ保持を追加。 |

**場所:** 新規 `src/00_OutputFiles.cs`、`src/04_HostServices.cs`、`src/08_BookIO.cs`。

同じ実行で作った出力だけを再ビルドの置換対象とする設計は維持しています。
元のブックを上書きしない設計も維持します。ファイルシステムの非対応、外部アプリとの
競合、強制終了、電源断まで含む完全なトランザクション保証ではありません。
Windows の実 I/O 試験は未実施です。

### B05 — 依頼を変えても古い返答で進める【高】

発行済みの依頼文を編集しても、依頼番号・取り込み済み返答・出力フォルダ・コピー済み表示が
有効なままでした。「画面にある依頼」と「書き戻す回答」が違う状態になり得ます。

発行後の内容・返答形式の変更では依頼番号を更新し、旧返答、分割の途中状態、準備済み
ファイルへの参照、コピー済み表示、前回のビルド結果を無効化します。
テンプレート内の依頼番号も更新します。同じ内容の入力イベントだけでは無効化しません。
目的・モード・ブックを変更した場合の関連状態も整理しました。

**場所:** `assets/js/state.js`。**確認:** Node とブラウザーの両方で確認済み。
旧実行フォルダの実ファイルは削除せず、新しい依頼を作ると別のフォルダになります。

### B06–B08 — 不正・混在した AI 返答を受け入れる【中〜高】

| 番号 | 元の問題 | 修正・確認 |
|---|---|---|
| B06 | `COMPLETE` がモジュールより前にある、終了宣言が重複する、終了後に概要がある、モジュール数が `0x1` / `1e0` / 余分な引数付きでも受理される場合がある。 | 終了宣言の位置・重複・引数・十進整数を厳格化。概要の重複、モジュール内の矛盾した `NOCHANGE` も拒否。正常な LF / CRLF / CR とコードフェンス付き返答は維持。Node で確認。 |
| B07 | 新規モジュール名 `constructor` が JavaScript の継承プロパティと衝突し、既存の Object コンストラクターをモジュールとして扱い得る。 | モジュール名・重複判定等の辞書を継承プロパティのないオブジェクトに変更。新規標準モジュールとして正常判定するテストを追加。 |
| B08 | 完成した旧返答がある状態から新たな分割返答の収集を始めると、旧返答の進行条件が残る場合がある。 | 有効な新規部分を受け入れる際は古い完成結果を無効化。全パートが揃うまで次へ進めない。画面の「最初から取り込み直す」経路も確認。 |

**場所:** `assets/js/response-package.js`、`assets/js/app.js`、`assets/js/host-bridge.js`。
不正な一括返答を取り込もうとしただけで、既に有効な返答を消さない動作は維持しています。

### B15–B16 — 元ブックの整合性・不完全な読み取り【高】

**B15:** 検証用の読み取り後、コピー時に元ブックを再度開いていたため、間で保存が入ると
検証した VBA とコピーしたブックの世代が異なる可能性がありました。
また、変更検知が主に表示用のコード本文を対象とし、先頭属性や管理情報を十分に含めていませんでした。

一度読み取ったバイト列を検証・コピー双方に使います。変更検知には `FullCode`、
種類・名前、コードページ、`PROJECT`、展開した `dir` も含め、SHA-256 で識別します。
保守的な検査なので、ソースが同じでも Excel が管理情報を再構成した場合は再読み込みを
求めることがあります。Excel が読み取り中に保存する状況を完全に同期させる仕組みではありません。

**B16:** 部分読み取り・救出結果でも AI 向けに完全性を強く主張し、ビルドへ進むと
利用者が「完全な元コードに対する修正」と誤解する可能性がありました。

`SOURCE_READ_STATUS` をテンプレートへ追加し、依頼文とソースファイルに不確実性を明記。
`sourceDoubt` のブックでは改修を早い段階で止め、相談モードまたは正常な元ブックの
再読み込みへ案内します。バックエンドでも `E-BUILD-05` によりビルドを拒否します。
管理情報のみの警告は区別し、一律に禁止しません。

**場所:** `src/08_BookIO.cs`、`src/04_HostServices.cs`、`assets/js/screens.js`、
`assets/js/prompt-template.js`、`assets/js/app.js`、`templates/request-template.txt`。
画面の進行制御と警告文はテスト済み。C# のスナップショット・書き戻しは未実行です。

## 4. その他の修正一覧

| 番号・重要度 | 問題・使用条件 | 修正 | 確認の種類 |
|---|---|---|---|
| B09・中 | 再度依頼を生成すると、前回 AI が追加したモジュールまで「元ブックのソース」に混ざり得る。 | 出力対象を `getBookModules()` に統一。元ブック由来のみを添付用ソースにする。 | Node、コード確認 |
| B10・低 | 末尾改行等の正規化だけで「変更あり・差分 0 行」という紛らわしい状態になる。 | 正規化後に同一なら元の文字列を保持。手動修正の受入れにも同じ基準を適用。 | Node |
| B11・中 | 探索範囲を超える長い挿入・削除で、以降の同一部分まで変更として数える。150 行追加の例で 350 行と表示。 | 近傍に一致がない場合、残りの行から一致点を探すフォールバックを追加。 | 150 行追加・削除、500 ケースの復元性テスト |
| B12・中 | `NUL.xlsm`、`CON.backup.xlsm`、制御文字を画面が受理する一方、合法な `report..xlsm` をホストが拒否。長い既定名は自身の 120 文字制限に抵触する。 | Windows 予約名・制御文字・拡張子等を UI / ホストで整合 [S4]。合法な内部の連続ドットを許可。既定出力名を 120 文字以内、実行フォルダの表示名を 48 文字以内に調整。名前エラーを画面に表示。 | Node、画面テスト。ホストは静的確認 |
| B13・高 | 2 分の読み込みタイムアウト後もホストが動き続け、別操作との整合性が崩れる可能性。ファイル選択を長く開くだけでもタイムアウトする。 | 読み込み・ファイル選択の期限で完了を決めず、ホストの応答を待つ。読み込みの遅延通知とホスト側の処理中ガードを追加。 | コード確認。実 Windows の長時間試験は未実施 |
| B14・中 | ビルド前の非同期アセット読み込み中に busy が立たず、重複ビルドを要求できる。 | 最初の非同期処理より前に busy を設定。 | ブラウザーで二重呼出し時のホスト要求が 1 件と確認 |
| B17・中 | 相談モードの依頼文にも「改修してください」という共通指示が混ざる。 | 共通テンプレートを中立化し、相談・診断では指示のないコード改修を行わないと明記。 | Node、相談フローの画面テスト |
| B18・中 | 複数ブックをドロップすると、先頭だけが黙って選ばれる。 | Web / ホストイベントの両経路で複数入力を拒否し、1 つずつ選ぶよう通知。処理中のドロップも明示的に拒否。 | ブラウザーのホストイベント、コード確認 |
| B19・中 | 固定の最小ウィンドウサイズが拡大表示の画面に収まらない。フォームが縮んで入力欄が切れ、通知が取り込みボタンを遮る。 | WPF の初期サイズを作業領域内に収める。通常フォームを縦スクロール可能にし、通知のマウス遮蔽をなくす。 | 760 × 440 のブラウザー領域で入力欄・次へ操作確認。WPF / DPI 実機は未実施 |
| B20・低 | 対応形式と表示している `.xls` が選択ダイアログの主フィルターにない。 | `.xls` を主フィルターへ追加。 | 静的確認 |
| B21・中 | 最初の WebView ナビゲーションが失敗してもスプラッシュを消していた。 | 成功状態を確認し、失敗時は起動エラーを表示して終了。 | 静的確認 |
| B22・中 | 読み込み・書き戻し中の通常終了により、ワーカー処理が途中で終了し得る。 | ホストエンジン処理中のウィンドウ終了を抑止し、待機を案内。 | 静的確認。強制終了・電源断は対象外 |
| B23・中 | 宣言コードページが使えないとき、フォールバックエンコードが表現不能文字を置換し得る。 | フォールバックでも Encoder / Decoder の例外設定を有効化し、黙った文字化けを避ける。 | 静的確認、Windows 用の試験を用意 |

## 5. テスト結果と根拠ファイル

| 検査 | 結果 | 根拠 |
|---|---|---|
| 提供 ZIP に対する回帰テスト | 92 件中、成功 58 / 失敗 34 | `tests/results/javascript-before.tap` |
| 修正版の同一回帰テスト | **92 件中、成功 92 / 失敗 0** | `tests/results/javascript-after.tap` |
| 修正版の Chromium 画面テスト | **13 シナリオ成功**、捕捉した未処理 JavaScript 例外なし | `tests/results/browser-after.txt` |
| 差分のランダム入力 | 固定シード 500 ケースで左右の元入力を復元可能 | 92 件に含まれるテスト。最小差分の数学的保証ではない |
| JS 構文チェック | `assets/js` の全 `.js` が `node --check` を通過 | 実行確認 |
| プリセット | 同梱 6 件の構造を検証 | JavaScript テストに含む |
| 元サンプルと過去生成物のキャッシュ比較 | ソース関連ストリーム変更、プロジェクトキャッシュ全体一致を確認 | `tests/results/sample-cache-inspection.json` |
| Windows / C# 検証 | **未実行**。全ソースのコンパイルと 12 グループのコア試験を用意 | `tests/Test-Windows.ps1` / `tests/CoreRegression.cs` |
| Excel 再オープン・コンパイル・実行 | **未実行** | `tests/README.md` の実機チェックリスト |

34 件の失敗は、34 種類の独立したバグという意味ではありません。予約ファイル名など、
1 つの原因に対する複数の入力例を含みます。また、テストが通ることは未列挙の使い方での
不具合がないことの証明ではありません。

## 6. 用途への柔軟性と、残る制約

### 維持・改善した用途

通常の一括改修、簡易モード、コードを変えない相談・診断、長い返答のモジュール単位
取り込み、変更不要 / 改修不可能という返答、既存の標準・クラス・フォーム・
ドキュメントモジュールの変更、標準モジュールの新規追加、手動修正、任意の依頼文編集を
維持しました。用途を切り替える際の古い状態の混入と、相談指示の矛盾を修正しています。

### 残る制約・注意点

| 項目 | 残る制約・必要な確認 |
|---|---|
| Windows / Excel | この修正版の実機検証は未完了。特にキャッシュ無効化後の再コンパイル、出力 ZIP / OLE2 の読み戻し、`File.Replace` の実 I/O を確認する必要があります。 |
| ブック形式 | `.xlsm` / `.xlam` / `.xlsb` / `.xls` は入力対象ですが、すべての派生形式・Excel バージョンを保証しません。4096 バイトセクターの複合ファイルの書き戻しは未対応です。 |
| 保護・署名 | 開くためのパスワードで暗号化されたブックは読み取り不可。VBA 閲覧保護は実ファイル検証が必要。コード変更後の署名の有効性は維持せず、再署名機能もありません。 |
| 構造変更 | 新しいクラス / UserForm / シート、モジュールの削除・名前変更、フォーム設計や参照設定を変更する機能は追加していません。既存の画面でも新規標準モジュール以外は拒否します。 |
| 非表示属性 | 先頭に連続する Attribute 行は保持しますが、プロシージャ属性など本文中の Attribute 行は本文として AI の返答の影響を受けます。属性全般が必ず保全されるとは言えないため README を訂正しました。 |
| カスタムプリセット | 所定の見出し・用途・HTML コメントの独自ルールがあります。任意の Markdown 構文を扱う汎用パーサーではなく、閉じ忘れたコードフェンス等の診断は十分ではありません。複雑なコード例を含む独自プリセットは、生成依頼文を確認してください。 |
| 大量データ | 元ブック・ソース・差分をメモリー上に保持します。大容量ブック、極端に長い 1 行、巨大な返答に対するメモリー上限やキャンセル機能は追加していません。 |
| パス・保存先 | 元ブックと同じ場所の `MacroStudio` 配下へ保存する構成です。任意の別フォルダへの出力先選択は追加していません。フルパス長、書き込み権限、ネットワークドライブ・同期フォルダの制約は残ります。 |
| 再ビルド | 同じ実行で作成した成果物は置換されます。生成後に他のアプリで変更した内容までは追跡しないため、残したい生成物は別名コピーを作ってください。 |
| 差分表示 | 改行や末尾空白の正規化が入り、必ず最小編集距離の差分になるわけではありません。業務上の意味や VBA の構文正当性を解析する機能ではありません。 |
| 強制終了 | 通常の終了は処理中に抑止しますが、OS の強制終了・電源断を完全に防ぐことはできません。残った一時ファイルやバックアップは、内容を確認してから扱ってください。 |
| 過去の同梱生成物 | `sample-book/MacroStudio` の過去の `*-Modified-*.xlsm` は原本を維持しており、今回修復・再生成したブックではありません。古いキャッシュ残存の注意書きを追加しました。 |

## 7. パッケージの変更範囲

主な変更ファイルは次のとおりです。

| 分類 | ファイル |
|---|---|
| 保存・VBA | 新規 `src/00_OutputFiles.cs`、`src/04_HostServices.cs`、`src/07_VbaProject.cs`、`src/08_BookIO.cs` |
| ウィンドウ・ホスト | `src/02_MainWindow.cs`、`src/03_MessageRouter.cs` |
| 状態・解析・画面 | `assets/js/state.js`、`response-package.js`、`host-bridge.js`、`app.js`、`screens.js`、`prompt-template.js`、`diff.js` |
| 表示・テンプレート | `assets/css/layout.css`、`flow.css`、`variables.css`、`assets/index.html`、`templates/request-template.txt` |
| 文書・検証 | `README.md`、本報告書、`tests/`、過去生成物への注意書き |

ランチャーは `src/*.cs` を列挙してコンパイルするため、新規 `00_OutputFiles.cs` は
既存の起動方法で読み込まれます。別のインストーラーや SDK 依存は追加していません。
同梱 DLL、ライセンス、元のサンプルブック・過去成果物のバイト内容は変更していません。
元 ZIP 自体も変更していません。

**フォント本体は修正 ZIP に含めていません。** Noto Sans JP / UDEV Gothic が端末に
インストールされていればそれを使い、それ以外はシステムフォントへフォールバックします。
CSS からフォントファイル取得を外し、外部ダウンロードは追加していません。
以前のフォントライセンス文書は保管しました。字幅や折り返しは端末により変わります。

## 8. 再確認手順

最初に `tests/README.md` を参照してください。JavaScript は追加 npm パッケージなしで
`node --test tests/test-js.cjs` を実行できます。

Windows 上では `powershell.exe -NoProfile -STA -File .\tests\Test-Windows.ps1` により、
全 C# ソースのコンパイルと、コピーしたサンプルへの読み書き検査を実施します。
このスクリプトは Excel / VBA を実行しません。成功後も、マクロを無効にして出力ブックを
開く、VBE でコンパイルする、差分と参照設定を確認する、安全と確認したコピーで業務操作を
確認する、という実機確認が別途必要です。

## 9. 参照した一次資料

外部仕様の根拠は以下です。不具合の再現結果自体の根拠は、提供ソースと同梱テストログです。

- **[S1] Microsoft MS-OVBA — Versioning and Localization**  
  https://learn.microsoft.com/en-us/openspecs/office_file_formats/ms-ovba/4d20e95c-0c17-47d6-84b9-8da95d8ea129
- **[S2] Microsoft MS-OVBA — `_VBA_PROJECT` Stream**  
  https://learn.microsoft.com/en-us/openspecs/office_file_formats/ms-ovba/ef7087ac-3974-4452-aab2-7dba2214d239
- **[S3] Microsoft .NET — `File.Replace` / `File.Move`**  
  https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace  
  https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move
- **[S4] Microsoft Win32 — Naming Files, Paths, and Namespaces**  
  https://learn.microsoft.com/en-us/windows/win32/fileio/naming-a-file

---

本報告書では「実装修正」「この環境で実行した検査」「Windows / Excel で残る検査」を
区別しています。実機未検証部分を含むため、修正 ZIP をそのまま本番の唯一のブックに
適用せず、必ずコピーで確認してください。
