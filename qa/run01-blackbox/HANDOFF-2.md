> **【後任追記 2026-08-03】この文書のあとに続きがあります。まず `HANDOFF-3.md` を読んでください。**
> 本文はそのまま残してあります（PROD-12 の格上げ・PROD-13/14 の新規・commit/push 境界の変更は HANDOFF-3 側）。

# 引き継ぎ書 その2 — MacroStudio 実機ブラックボックス試験（run01 続き）

- 宛先: **まっさらな新規セッション**（前提知識ゼロで読める前提）
- 前任: opus-qa-supervisor（`HANDOFF.md`）→ 本セッション → **あなた**
- 対象: `<repo>` の**未コミット作業ツリー**（`main` / `fe4395a` からの差分）
- 証跡: `qa\run01-blackbox\`

**commit / push / deploy は一度もしていません。** 既存 dirty 差分は巻き戻していません。

`HANDOFF.md` の §6（ハーネスの使い方）・§7（安全境界）・§8（盲検AIの規律）は**そのまま有効**です。

---

# 0. 絶対命令（最初に読む。これに違反しないこと）

> ## **全てのタスクを終わるまで、途中で中断しないこと。**
> ## **進められるものを全て進めるまで、ユーザーへの質問もしないこと。**

これは希望ではなく**命令**です。前任も本セッションも繰り返し破りました。
安全境界に当たっていないのに、**報告を書くたびに一区切りついた気になって手を止めました**。

具体的に、次はすべて**違反**です:

- 「ここまでできました、続けますか？」と人へ返す
- 「残りは次のセッションで」と自分で決める
- 各画面・各項目が終わるたびに止まって報告する
- 判断に迷ったので聞く（**§5 の未修正3件のように、根拠を書いて先へ進む**）
- 待ち時間（盲検AIの回答待ちは実際に 6〜30 分ある）に手を止める
  → **その間は別の残件を進める**。本セッションは Excel COM の重い処理を全て background で走らせ、
     その裏で別作業をしていました。同じようにすること

**止まってよいのは `HANDOFF.md` §7 の安全境界に当たったときだけ。**
それ以外は全て「続ける」が正解です。文脈切れは停止理由になりますが、
**尽きる直前まで作業してから**引き継いでください。

**報告は走りながら出し、報告を区切りにしないこと。**

## 文脈を長持ちさせる方法（これが一番効きます）

GUI スクリーンショットが文脈を食い尽くします（前任は 40 枚以上読んで枯渇）。
**画像は「状態遷移・エラー・崩れの確認」に限り、値の照合はテキストで行う**こと:

| 目的 | 使う道具（GUI 不要） |
|---|---|
| 生成物が本当に動くか | `lib\smoke-corpus.ps1` — 全ブックの入口マクロを実 Excel で実行し postRun 照合 |
| 候補分類が正しいか | `lib\classify-corpus.ps1` — 実ひな形の規則を実リテラルへ当てて oracle と突合 |
| 製品の読み経路 | `lib\verify-g06-readback.ps1` — 製品の C# を `Add-Type` して直接叩く |
| 生成ブックの中身 | `lib\verify-output.ps1` / `lib\verify-ai-route.ps1` / `lib\dump-modules.ps1` |
| ストリームの所在 | `lib\cfb-scan.js` |
| UI ロジック | node テスト 49 本（`docs\DEVELOPMENT.md` に一覧） |

---

# 1. いま動いているもの

| 対象 | 状態 |
|---|---|
| MacroStudio `pid 1860` | **起動中**。1600x1050。画面0（`A05P_xlarge_with_path.xlsm` を読み込んだ状態）。`logs\app-state.json` はこれを指す |
| MacroStudio `pid 38960` `pid 61648` | **起動中**。前任が 20:27 / 20:32 に起動したものが残っている。§7 に従い触っていない |
| Excel | 0 件 |
| blind-ai-orchestrator | **本セッションでは一度も使っていない** |

> **プロセスの数え方に注意（一度間違えた）**
> ホストは `powershell.exe -File macrostudio.ps1` なので
> **`Get-Process -Name MacroStudio` は 0 件を返します**。正しくは:
> ```powershell
> Get-CimInstance Win32_Process -Filter "Name='powershell.exe'" |
>   Where-Object { $_.CommandLine -like '*macrostudio.ps1*' }
> ```

## ウィンドウは常識的なサイズで起動すること（先生の指示）

前任の `Set-WindowRect ... -W 2880 -H 1920` は**大きすぎます**。
`lib\_g16_launch_small.ps1` のように **1600x1050** で起動してください。

**幅 2000 以下では `act.ps1` の `CurrentScale` が 1.0 になり、画像座標＝ウィンドウ座標**です
（1.44 倍の換算が不要）。1600x1050 での確定座標:

| 画面 | 部品 | 座標 |
|---|---|---|
| 共通 | ［次へ］/［完了］ | `(1467,990)` |
| 共通 | ［戻る］ | `(1285,990)` |
| 0 | ドロップ枠 | `(800,470)` |
| 0 | ［選び直す］（読込済み） | `(1368,476)` |
| 1 | 「診断を飛ばして…」 | `(88,904)` |
| 1 | ［クリップボードから診断結果を取り込む］ | `(247,601)` |
| 3 | ひな形2番目「固定パス」 | `(88,439)` |
| 4 | ［置き換え後の値］ | `(750,593)` |

**座標を当てずっぽうで書かないこと。** 1 度やって file dialog が開かず 3 回空振りしました。

---

# 2. 決着済み（もう触らなくてよい）

| ID | 決着 | 実 GUI 確認 |
|---|---|---|
| PROD-01〜07・09 | 前任が修正済み | 済（PROD-05 / 07 は本セッションでも再確認） |
| **PROD-08** | 要約カードを「物理1行目」→「**最初の一文**」へ。SPEC §2.2 が根拠 | **済** `R2_005_S2_headline.png` |
| **PROD-10** | 容器として読めないファイルを `E-ATTACH-03`→**`E-ATTACH-02`**。SPEC §13.4「**どこにも VBA が無い場合だけ**」が根拠 | **済** `R2_001_G08_zero_bytes_post.png` |
| DOC-01 | README の成果物配置を実測どおりに（`exports\`+`temp\` の2本立て） | — |
| 観察「7件の指摘 vs 3件」 | **不具合ではない**と判定。SPEC §2.2 の「上部」＝結論バンドで、そちらは規定どおり | 済 |
| SPEC §15.2 (2) | VBA閲覧保護ブック `G06` を実作し、**製品は 3 モジュール完全に読める**ことを実証 | 済 |
| HARNESS-01 | `act.ps1`/`act2.ps1` の `Tap` に**ウィンドウ矩形内検査**を追加 | — |

## コーパスは **53/53 すべて現物あり**

`lib\make-corpus-rest.ps1`（24本）+ `lib\make-corpus-g06.ps1`（G06）で完成。
派生 `A05P_xlarge_with_path.xlsm` も追加（画面8を捕まえるための大規模＋固定パス試料）。

- 自己検証: **45 PASS / 3 FAIL / 5 SKIP** → FAIL 3 件は追跡して全て「試料側・環境側の事実」と確定
- 候補分類: **44 本 PROBLEMS 0**

## テスト状況

- **node 49/49 PASS**
- `tests\test-bookio.ps1` PASS（C# のコンパイル込み）

---

# 3. 変更したファイル（commit していない）

## 製品側
| ファイル | 変更 |
|---|---|
| `src/08_BookIO.cs` | `TryReadZipVbaProject` に `out bool archiveEnumerated`。容器として読めなければ `E-ATTACH-02`（PROD-10） |
| `assets/js/app.js` | `E-ATTACH-02` を `blockingAttachErrors` へ追加＋見出し＋文言を2原因に耐える形へ |
| `assets/js/screens/workflow.js` | `firstSentence` を追加し要約カード2か所で使用（PROD-08） |
| `tests/test-diagnosis-headline.js` | **新設** |
| `tests/test-output-name.js` | **新設**（画面7の受理条件を総当たり） |
| `tests/test-bookio.ps1` | 0バイト・偽装テキスト → `E-ATTACH-02` の2例を追加 |
| `tests/test-attach-blocked.js` | 「読めないファイルはトースト」→「カードが出る・マクロと言わない」へ |
| `docs/DEVELOPMENT.md` | 新設2テストを登録 |
| `README.md` | 「実行フォルダと成果物」節を実測どおりに |

**`docs/SPEC.md` は触っていません**（他参加者の dirty 差分が乗っているため。§5 の DOC-02 参照）。

## 証跡側の新規ツール（`run01\lib\`）
`make-corpus-rest.ps1` / `make-corpus-g06.ps1` / `smoke-corpus.ps1` /
`fix-corpus-smoke-fails.ps1` / `probe-path-limit.ps1` / `finalise-b05.ps1` /
`verify-g06-readback.ps1` / `_g01`〜`_g21`（GUI 操作の使い捨て）

---

# 4. 残件 — **これを全部やる**

上から順に価値が高い。**1 つ終わっても止まらず次へ進むこと。**

## 4.1 実 GUI の未確認・未操作（`ledger\component-ledger.md` 末尾に消化/未操作の別あり）

| # | やること | 備考 |
|---|---|---|
| 1 | **画面9 ［完了］の実挙動** | 最重要。実装（`app.js:3050 finishFlow()`）は状態を reset して画面0へ戻すだけ・**アプリは閉じない**。実クリックの取り直しが要る |
| 2 | 画面9 ［出力フォルダをエクスプローラーで開く］ | 開いた Explorer 窓は**自分で閉じる**（§7） |
| 3 | 画面9 ［このあと人が確かめること］15件 | 開閉 |
| 4 | 画面8 ［もう一度ビルドする］ | **先にビルドを失敗させる必要あり**。出力先に非所有の同名ファイルを置けば E-BUILD-03 を起こせる |
| 5 | 画面7 ［作成されるファイルの場所を見る］ | 未操作 |
| 6 | 画面7 予約デバイス名（`CON.xlsm`）・長大名の実投入 | PROD-12 の実挙動。ビルドまで進めて何が起きるか見る |
| 7 | 画面6 ［修正を反映］ | 未操作（［編集をやめる］側だけ済んでいる） |
| 8 | 画面6 モジュール一覧の切替 | **複数モジュールが変更される試料が要る**（AI 経路 or `D08`） |
| 9 | 画面2 アコーディオン | 「このマクロの詳細」4行・「見つかった事実」各行が**未開閉** |
| 10 | 画面1/5 分割受理・欠番・［最初から取り込み直す］ | 未着手 |
| 11 | 画面5 **質問返しの回答**・**NOCHANGE 3判断**（改修不要／改修できない／判断できない） | 未着手。試料は `H01` `H02` `H04` |
| 12 | `G07`（偽装）`G09`（途中切れ）の実機再投入 | PROD-10 と同じ経路だが実機では未再投入 |

### 画面8 を捕まえる方法（本セッションで確立）
`A05P_xlarge_with_path.xlsm`（33モジュール・12,041行）を**置換のみ経路**で走らせ、
［次へ］の **500ms 以内**に撮影する。12,041行でも 1 秒未満で終わるので遅れると画面9になる。
ビルド中は**［戻る］［次へ］とも無効**であることは確認済み。

### 効率的な経路
`S01_fixed_drive.xlsm` か `A05P` で**置換のみ経路**を通すと、盲検AIを待たずに
画面0→1→3→4→6→7→8→9 まで到達できる（画面5を通らない）。

## 4.2 コーパスの実 GUI 投入 — **53本中47本が未投入**

投入済みは `G08` `G06` `input_win32_sleep` `S01` `A05` `A05P` のみ。価値の高い順:

- `A04`（18モジュール5,200行）— **分割返答**の誘導
- `D08`（同一値40か所）— 集約表示・大量行スクロール・複数モジュール切替
- `C05`（イベント）/ `C07`（Attribute行）/ `A08`（空モジュール）— **往復で消えやすいもの**
- `H01` `H02` `H04` — NOCHANGE 3判断と、**質問返しを製品が拒否するか**（H04）
- `B05`（全長245文字）— 長パスの実入力
- `G01 .xlam` / `G02 .xlsb` / `G03 .xls` — 形式3種の実 GUI 投入
- `B02` `B04` `B06` `C01`〜`C04` `C06` `C08` `D01`〜`D07` `D09` `D10` `E01`〜`E04` `F01`〜`F05` `H03`

## 4.3 未修正の 3 件 — **判断して直すか、直さない根拠を書く**

いずれも**安全は壊れていない**。詳細は `findings\FINDINGS.md`。

| ID | 内容 | 本セッションで直さなかった理由 |
|---|---|---|
| **PROD-11** | 置換候補に ProgID（`WScript.Shell`）・コマンド行（`cmd.exe /c echo hello`）・ワイルドカード（`*.xlsx`）・裸の拡張子（`.csv`）が混じる | 規則を締めると `Report.Backup` のような**本物のファイル名を取りこぼす**リスク。取りこぼしの方が危険で、候補は既定チェックでもない。**出荷見本 S01 でも再現する**ので優先度は上げてよい |
| **PROD-12** | 画面7が `CON.xlsm` 等の予約デバイス名・制御文字を通す | 落ちるのはビルドで、E-BUILD-03 が出力を捨てるので安全。画面で弾くかは製品側の線引き |
| **DOC-02** | SPEC §15.2 が**実装に無い `E-ATTACH-05`** を参照。同項の「.xlsb の fixture が無い」も今は古い（`G02` を実作済み） | `docs/SPEC.md` に他参加者の dirty 差分が乗っているため触っていない |

**新規の観察（まだ ID を振っていない）**:
- 画面7 の不正値は**枠が赤くなるだけで理由が出ない**。唯一の補足文が
  「拡張子は .xlsm のままにします」で、パス区切りが原因のときは**実際の理由と食い違う**
- 画面9 から［戻る］を連打すると**画面0まで戻り切る**（ブックは保持）。
  成果物は書き出し済みなのに画面は何事もなかったように戻る。SPEC に規定が見当たらない

## 4.4 その他

- `G06` の**書き戻し後**に閲覧保護が残るか（読み取りは確認済み。ビルド後は未確認）
- **署名付きブックの Excel 実挙動**（SIG-01。`HANDOFF.md` §7 の境界事由。**変化なし**）
- **PowerShell 側テストの全実行**（本セッションは `test-bookio.ps1` のみ。`docs\DEVELOPMENT.md` に一覧）
- **大量反復**（同じ試料を順序・状態を変えて繰り返す）— 依然ほぼ未着手
- **盲検AI** — 本セッションは一度も使っていない。§8 の規律は厳守（新規個体・逐語・ツール無し）
- 提案: `F02` の変種として **ACE/Jet 接続文字列にファイルパスが埋まった本**を足すと、
  「長いリテラルの内側にある固定パス」という実務で最も多い形を検査できる

---

# 5. 本セッションで測った環境の値（そのまま使えます）

| 事項 | 実測 |
|---|---|
| Excel が開けるフルパス長 | **254 文字は開ける / 256 文字で失敗**。俗説の 218 文字とは一致しない |
| MAX_PATH 超（269文字） | Excel も .NET も開けない。`\\?\` 前置でも Excel は不可 |
| VBA と Unicode | 絵文字・CJK拡張B・結合文字は**1つも往復できない**（B06）。**原理的に検査不能** |
| 全角記号の識別子 | 保存後に別表記へ正規化される（B02） |
| `PtrSafe` 無し `Declare` | 64bit Excel では**モジュールごとコンパイル不能**。E01/E04 の入口マクロは改修後にしか動かない |
| ビルド速度 | 12,041 行で **1 秒未満**。SPEC §15.2(8) の「120秒超のビルド」はこの規模では再現不能 |
| VBA の 1 物理行 | 約 1,023 文字 |

---

# 6. 踏んだ罠（`HANDOFF.md` §6 に追加）

9. **Write ツールで制御文字をソースに直書きしない** — `\u0001` のつもりが実バイトで入り、
   以後その行が Edit で一致しなくなる。`String.fromCharCode(1)` を使う
10. **oracle の JSON も UTF-8 BOM 付きで書く** — PS5.1 側で読むため
11. **`New-Object 'byte[]' 0`** — 引用符を外すと型名の解釈で失敗する
12. **Excel の `Workbooks.Open` は `\\?\` 前置を受け付けない** — 長パスは .NET でしか触れない
13. **`Get-Process -Name MacroStudio` は 0 件を返す** — ホストは `powershell.exe`（§1）
14. **ウィンドウが縮むと `CurrentScale` が 1.44→1.0 に変わり、古い座標が窓外へ出る**
    — HARNESS-01 として `Tap` に矩形検査を入れたので今は例外で止まる
15. **座標を推測で書かない** — 撮って読むこと。1 度やって空振りした

---

# 7. 申し送り（前任＋本セッション。依然として有効）

- **「テストが緑」と「実物が直っている」は別物**。PROD-10 も実 GUI で見て初めて閉じた
- **製品の自己検証を根拠にしない**。必ず実 Excel で開き、コンパイルし、実行し、閉じて再度開く
- **推測で断定しない**。E01/E04 の失敗を「PtrSafe だろう」で済ませず、
  使い捨てコピーに PtrSafe を付けて実行できることを確かめてから oracle に書いた
- **自分の oracle を疑う**。F02 の候補0件は取りこぼしに見えたが、本文を出したら
  パスを含まない SQL Server 接続文字列で、**0件が正しかった**
- **黙って return するコードを疑う**。PROD-04 / PROD-09 はどちらもそれが原因だった
- 未完了は件数でごまかさず、**具体的な阻害条件**とともに残す
- **そして何より — 途中で止まらない。全部終わるまで質問もしない。**
