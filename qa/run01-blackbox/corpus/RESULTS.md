# コーパス投入結果

- 現物: `corpus\books\` **29 本** / oracle: `corpus\oracles\` **29 件**
- 生成器: `lib\make-corpus.ps1`（A/B/C/D 基本）/ `make-corpus-boundary.ps1`（形式・拒否系）/ `make-corpus-d.ps1`（D/E/F/H）
- 生成しただけでは合格にしていない。下表は**実 GUI 投入**または**実 Excel 検証**の結果

## 生成物の自己検証（実 Excel）

12/12 PASS（A01 A06 A07 B01 B03 C01 C02 C03 C06 C08 D01 D02）
検査: oracle のモジュール集合が実在 / 入口マクロが実行できる / 期待セル値が一致

## 実 GUI 投入（画面0 → 画面4 の候補判定）

| ID | 内容 | 結果 | 証跡 |
|---|---|---|---|
| D07 | ドライブ・UNC・相対・URL・**書式文字列**の全混在 | **4種類・4か所。書式は1つも出ない** `S:\eigyo\`=ドライブ(既定チェック) / `\\srv\share\`=ネットワーク(既定チェック) / `..\out\`=連結断片(未チェック) / `https://…`=URL(未チェック) | `CORP_D07_candidates.png` |
| D09 | `"S:\" & folder & "\out.csv"` の連結 | **2種類・2か所**。`S:\`（ドライブ）と `\out.csv`（連結断片）を**別々のリテラル**として提示。連結を復元しての部分置換をしない | `CORP_D09_candidates.png` |
| D03 | UNC 複数 | 撮影済み・要目視 | `CORP_D03_candidates.png` |
| D04 | 相対パス（制限対象） | 撮影済み・要目視 | `CORP_D04_candidates.png` |
| D05 | URL / SharePoint 断片 | 撮影済み・要目視 | `CORP_D05_candidates.png` |

**D07 は PROD-03 修正の実地回帰**でもある。`yyyy/mm/dd` と `0.00` を同じブックに入れてあるが、候補に一切現れない。

## 実 GUI 投入（拒否系・画面0）

| ID | 中身 | 出たもの | 判定 |
|---|---|---|---|
| G04 | マクロ無し `.xlsx` | `E-ATTACH-03`「このブックにはマクロがありません」 | **妥当** |
| G05 | ブック全体を暗号化 | `E-ATTACH-04`「ファイル全体がパスワードで暗号化…外したコピーを保存してください」 | **妥当・適切に区別** |
| G07 | テキストを `.xlsm` と偽装 | `E-ATTACH-03`「マクロがありません」 | **PROD-10**（拒否は正しいが文言が誤誘導） |
| G08 | 0 バイト | `E-ATTACH-03`「マクロがありません」 | **PROD-10** |
| G09 | 正常ブックの先頭 3KB | `E-ATTACH-03`「マクロがありません」 | **PROD-10** |

いずれもクラッシュせず、受理せず、［次へ］無効。**安全性は保たれている**。

## 3経路の完走（実 GUI ＋ 実 Excel）

| 経路 | 試料 | 結果 |
|---|---|---|
| 置換のみ | `S01_fixed_drive.xlsm` | 完走。`E:\` 有 / `S:\` 無、実行値 `E:\eigyo\shinsei\`、再open後も維持 |
| AI のみ | `input_win32_sleep.xlsm` | 完走。`Declare` 文が1つも残らず、`RunApplicationReview` 実行成功、再open後も維持 |
| 両方 | `S01_fixed_drive.xlsm` | 完走。**置換が AI 段を越えて生存**（`E:\` 有 / `S:\` 無）、`DefaultExportFolder()` = `E:\eigyo\shinsei\`、`RunSummary` 実行、`作業!B2 = 集計済み`、再open後も維持 |

## 盲検 AI 回答のフェンス形態（同一設定でも揺れる）

| 試行 | フェンス | 受理 |
|---|---|---|
| C1 / C3 | 裸の ``` | ○ |
| C4 | **フェンス無し** | ○ |
| C6 | ` ```vba `（言語タグ付き） | ○ |

`'@MACROSTUDIO` 区切りで判定する実装は、この揺れに対して頑健。

## 候補分類の一括検証（`lib\classify-corpus.ps1`・テキストのみ）

**実ひな形 `02_固定パスを新環境へ置き換える.md` の規則を、実ブックから取り出した文字列リテラルへ適用**し、
各 oracle の `mustFix` / `mustPreserve` と突き合わせた。コメント行は除外。**20 書籍・PROBLEMS 0**。

| ID | 候補（`*`＝既定チェック） |
|---|---|
| A01 A06 A07 B01 B03 C01 C02 C06 **C08** F01 F02 H03 | **0 件** |
| D01 | `S:\eigyo\shinsei\`[ドライブ*] / `log.csv`[ファイル名] |
| D02 | `T:\eigyo\shinsei\`[ドライブ*] / `log.csv`[ファイル名] |
| D03 | `\\fileserver\share\`[ネットワーク*] / `\\srv2\keiri\`[ネットワーク*] |
| D04 | `..\data\`[連結断片] / `.\config\`[連結断片] — どちらも**未チェック** |
| D05 | `https://contoso.sharepoint.com/sites/eigyo/`[URL] / `/Shared Documents/`[連結断片] — **未チェック** |
| D07 | `S:\eigyo\`[ドライブ*] / `\\srv\share\`[ネットワーク*] / `..\out\`[連結断片] / `https://…`[URL] |
| D09 | `S:\`[ドライブ*] / `\out.csv`[連結断片] |
| G01 G02 G03 | `S:\eigyo\shinsei\`[ドライブ*] — **.xlam / .xlsb / .xls の3形式とも検出** |

- **C08 が 0 件**（`yyyy/mm/dd` `0.00` `yyyy/mm/dd hh:mm:ss` しか無いブック）＝ PROD-03 修正がコーパス全体で有効
- **D07 も書式を1つも拾わない**（同じブックに書式とパスが同居していても混ざらない）
- **D09** は `S:\` と `\out.csv` を別リテラルとして扱い、連結を復元しての部分置換をしない
- 相対・URL は候補に出るが**既定では未チェック**（制限対象の扱いが規則どおり）

**ORACLE 訂正 1 件（私の誤り）**: E01 の `mustFix` に `Declare` を書いていたが、
`Declare` は AI 経路（Win32 ひな形）で直す対象で、固定パス置換の候補ではない。
パス分類器に掛けていたのが誤り。oracle から除去し `oracleNote` に理由を記録した。

## 未投入（残件）

`G01 .xlam` / `G02 .xlsb` / `G03 .xls`（生成済み・GUI 未投入）
`E01` `F01` `F02` `H03`（生成済み・GUI 未投入）
`A01` `A06` `A07` `B01` `B03` `C01` `C02` `C03` `C06` `C08` `D01` `D02`（自己検証済み・GUI 未投入）
行列 63 本に対し現物 29 本。B05（200文字級ファイル名）、B06（絵文字）、D08（同一値40か所）、D10（260文字級）、
A04/A05（大規模）、G06（VBA閲覧パスワード）、H01/H02/H04 は未生成。

---

# 後任セッションでの続き（2026-08-02）

## 生成 — 行列 53 本のうち **52 本が現物になった**

前任の引き継ぎ書は未生成を 17 本と書いていたが、行列と現物を突き合わせると
**実際の未生成は 25 本**だった（`A02 A03 A08` / `B02 B04` / `C04 C05 C07` の 8 本が一覧から漏れていた）。
`lib\make-corpus-rest.ps1`（新設）で 24 本を生成し、`G06`（VBA 閲覧パスワード）だけが残っている。

- **生成 24/24 成功**、既存ファイルは 1 つも上書きしていない（skip 0 件＝全て新規）
- **自己検証**: `lib\smoke-corpus.ps1`（新設）で全コーパスの入口マクロを実 Excel で実行し
  postRun セルを照合 → **45 PASS / 3 FAIL / 5 SKIP**（SKIP は受理拒否試料で入口マクロが無いもの）
- FAIL 3 件はいずれも**製品ではなく試料側・環境側の事実**だった（下記）

| ID | 起きたこと | 追跡した結果 |
|---|---|---|
| `E01` | 入口マクロが実行できない | `PtrSafe` 無し `Declare` により 64bit Excel でモジュールごとコンパイル不能。**使い捨てコピーに `PtrSafe` を 1 箇所付けたら実行できた**ので原因確定。oracle に `entryRunsOnlyAfterRepair` を記録 |
| `E04` | 同上 | 同上（同じ実験で確認） |
| `B05` | Excel が開けない | フルパス長。**実測で 254 文字は開き 256 文字で失敗**。現物を全長 245 文字（ファイル名 173 文字）へ置き直し、開いてマクロが動くことを確認 |

## B05 の扱い（行列からの逸脱と、その理由）

行列は「200 文字級のファイル名」を求めているが、証跡ルート
`…\run01\corpus\books\b05\` だけで 71 文字あり、200 文字級のファイル名を足すと
**Excel が開けない全長**になる。よって:

- 現物は**開ける最長**（全長 245 / ファイル名 173 文字）に置いた
- 全長 269 文字の変種も `corpus\books\b05\` に残してある（Excel も .NET も開けない。証跡として保持）
- **200 文字級のファイル名そのもの**は、操作台帳の画面7「出力ファイル名（長大）」で検査する

## 候補分類の再実行（`lib\classify-corpus.ps1`・全 44 本）

**PROBLEMS 0**。新規本で確認できたこと:

- `D06` ワイルドカード → `S:\eigyo\torikomi\*.csv` を「ドライブから始まる場所」、
  `\\fileserver\kyoyu\*.xlsx` を「ネットワーク上の場所」で拾う（**期待どおり**）
- `D08` 同一値 40 か所 → 候補は **1 件に集約**される（表示は画面4で要確認）
- `E04` / `B06`（両方経路）→ `S:\eigyo\shinsei\` を既定チェックで拾う
- `A02`〜`A05` `B02` `B04` `C04` `C05` `C07` `E02` `E03` `F03` `H02` `H04` → 候補 0 件（**誤検出なし**）
- `F02` の候補 0 件は**正しい**。中身は `Provider=SQLOLEDB;Data Source=SRV01;…` で
  ファイルパスを含まない SQL Server 接続文字列だった（実際に本文を出して確認）
- `F04` / `F05` / `H01` で **PROD-11**（ProgID・コマンド行・ワイルドカードの誤検出）を検出。
  詳細は `findings\FINDINGS.md`

## 生成時に測った VBA 側の限界

| ID | 実測 |
|---|---|
| `B06` | 絵文字・CJK 拡張B・結合文字は **1 つも往復できない**（保存時点で化ける）。VBA コードページの限界 |
| `B02` | 全角記号を含む識別子（`合計＿金額`）は保存後に別表記へ**正規化**される |
| `D10` | 生成したパスリテラルは 263 文字（1 物理行の上限 1,023 には収まる） |

## 残件（このセッション終了時点）

- **`G06` も生成できた（`lib\make-corpus-g06.ps1`）。行列 53 本すべてが現物になった。**
  `PROJECT` ストリームの `DPB` を**同じ長さのまま**書き換える方法で、CFB 構造は無改変。
  Excel で開けて `VBProject.Protection=1` / `VBComponents=0`＝閲覧保護が実際に掛かっている。
  さらに製品の読み経路を `Add-Type` で直接呼び、**保護なしの元ブックと 1 文字も違わずに読める**ことを確認し、
  **SPEC §15.2 (2) の「未検証」を実ファイルで解消**した（`findings\FINDINGS.md`）
- **GUI 未投入**: 新規 25 本すべて＋前任から引き継いだ 15 本
- 提案: `F02` の変種として **ACE/Jet 接続文字列にファイルパスが埋まっている本**を足すと、
  「長いリテラルの内側にある固定パス」という実務で最も多い形を検査できる（現行 F02 では届かない）
